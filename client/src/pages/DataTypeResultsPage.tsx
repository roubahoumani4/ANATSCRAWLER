import React from "react";

const DataTypeResultsPage: React.FC = () => {
  return (
    <div className="p-8 min-h-screen bg-jetBlack text-coolWhite">
      <div className="max-w-4xl mx-auto">
        <h1 className="text-2xl font-semibold mb-2">Data Type Results</h1>
        <p className="text-sm text-gray-400">This page will display results for a specific data type from a scan. The OSINT engine integration is not configured in this environment.</p>
      </div>
    </div>
  );
};

export default DataTypeResultsPage;
